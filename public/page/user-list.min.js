/*!
 * CirebonWeb (https://cirebonweb.com)
 * Copyright (c) 2023 CirebonWeb
 * Licensed under MIT (https://opensource.org/licenses/MIT)
 */

// Load script setelah DOM berhasil dimuat
$(function () {
    // Elemen form modal
    let $formAddUser = $("#formAddUser");
    let formCekAddUser = FormHelper.cekFormData($formAddUser);
    let $formEditUser = $("#formEditUser");
    let formCekEditUser = FormHelper.cekFormData($formEditUser);

    // Fungsi load statisik user
    function loadStats() {
        $.get(window.routes.statisticUser) // Mengambil route endpoint yang telah didefinisikan pada global window variabel app.blade.php
            .done(function (res) {
                $(".stat-value").each(function () {
                    let key = $(this).data("key");
                    if (res[key] !== undefined && res[key] !== null) {
                        $(this).text(res[key]);
                    } else {
                        $(this).text("-");
                    }
                });
            })
            .fail(function (xhr, status, error) {
                $(".stat-value").each(function () {
                    $(this).text("-");
                });
                console.warn("Gagal memuat statistik:", error || status);
            })
            .always(function () {
                $(".overlay.dark").fadeOut();
            });
    }

    // Inisialisasi data tabel user-list
    TableHelper.init("#tableUserList", {
        ajax: window.routes.dataTable, // Mengambil endpoint url data tabel dari global window variabl
        order: [[1, "asc"]],
        filters: [3, 4], // filter untuk Status & Role saja
        buttons: [
            {
                text: "Add",
                attr: {
                    "data-target": "#modalAddUser",
                    "data-toggle": "modal",
                },
            },
            { text: "Excel" },
        ],
        columns: [
            {
                data: "id",
                title: "ID",
                className: "dt-head-center dt-body-center",
                orderable: true,
            },
            {
                data: "name",
                title: "Nama User",
                className: "dt-head-center",
                orderable: true,
            },
            {
                data: "email",
                title: "Alamat Email",
                className: "dt-head-center",
                orderable: false,
            },
            {
                data: "status",
                title: "Status",
                className: "dt-head-center dt-body-center col-status",
                orderable: true,
                render: {
                    _: (statusValue) => (statusValue ? "Aktif" : "Non aktif"), // representasi data yang tampil untuk semua versi render
                    display: (
                        statusValue // representasi data yang tampil untuk versi tampilan di browser saja
                    ) =>
                        statusValue
                            ? '<span class="lencana bg-primary">Aktif</span>'
                            : '<span class="lencana bg-danger">Non aktif</span>',
                },
            },
            {
                data: "role",
                title: "Hak Akses",
                className: "dt-head-center dt-body-center",
                orderable: true,
            },
            {
                data: "last_login_at",
                title: "Terakhir Aktif",
                className: "dt-head-center dt-body-center",
                orderable: false,
                render: TableHelper.tabelTanggalWaktu,
            },
            {
                data: "created_at",
                title: "Dibuat",
                className: "dt-head-center dt-body-center",
                orderable: false,
                render: TableHelper.tabelTanggalWaktu,
            },
            {
                data: "updated_at",
                title: "Diubah",
                className: "dt-head-center dt-body-center",
                orderable: false,
                render: TableHelper.tabelTanggalWaktu,
            },
            {
                data: "aksi",
                title: "Aksi",
                orderable: false,
                className: "dt-head-center dt-body-center",
                render: (data, type, row) => (type === "display" ? data : ""),
            },
        ],
    });

    // Ambil user berdasarkan id
    $(document).on(
        "click",
        '#tableUserList button[data-target="#modalEditUser"]',
        function () {
            let id = $(this).data("id");
            let url = window.routes.getUser.replace(":id", id); // Mengambil route endpoint yang telah didefinisikan pada global window variabel app.blade.php
            let $form = $("#formEditUser");

            $.get(url)
                .done(function (res) {
                    let user = res.user;

                    $form[0].reset();
                    $form.validate().resetForm();
                    $form
                        .find(".is-valid, .is-invalid")
                        .removeClass("is-valid is-invalid");

                    // Mendefinisikan endpoint route dari global window variabel app.blade.php untuk edit user
                    $form.attr(
                        "action",
                        window.routes.updateUser.replace(":id", id)
                    );

                    $("#edit_name").val(user.name);
                    $("#edit_email").val(user.email);
                    $("#edit_aktivasi").val(user.active ? "Aktif" : "Nonaktif");
                    $("#edit_role").val(user.role);
                    $("#edit_password, #edit_password_confirmation").val("");

                    if (user.profile) {
                        $("#edit_perusahaan").val(
                            user.profile.perusahaan || ""
                        );
                        $("#edit_whatsapp").val(user.profile.whatsapp || "");
                        $("#edit_telegram").val(user.profile.telegram || "");
                        $("#edit_alamat").val(user.profile.alamat || "");
                    } else {
                        $(
                            "#edit_perusahaan, #edit_whatsapp, #edit_telegram, #edit_alamat"
                        ).val("");
                    }

                    // insialisasi ulang data untuk deteksi perubahan Form Edit
                    formCekEditUser = FormHelper.cekFormData($form);

                    $("#modalEditUser").modal("show");
                })
                .fail(function (xhr) {
                    let message =
                        (xhr.responseJSON && xhr.responseJSON.message) ||
                        "Terjadi kesalahan saat mengambil data user.";

                    Swal.fire({
                        icon: "error",
                        title: "Gagal memuat data!",
                        text: message,
                    });
                });
        }
    );

    // Tambah user
    $formAddUser.validate(
        $.extend(FormHelper.validasiForm(), {
            rules: {
                name: { required: true, minlength: 3, maxlength: 30 },
                email: { required: true, email: true, customEmail: true },
                password: { required: true, minlength: 8 },
                password_confirmation: {
                    required: true,
                    equalTo: "#formAddUser input[name='password']",
                },
                perusahaan: { minlength: 3, maxlength: 30 },
                whatsapp: { number: true, minlength: 8, maxlength: 15 },
                telegram: { number: true, minlength: 8, maxlength: 15 },
                alamat: { minlength: 5, maxlength: 100 },
            },

            submitHandler: function (form) {
                if (!formCekAddUser.isChanged()) return false;
                return FormHelper.submitForm(
                    form,
                    $(form).attr("action"),
                    $(form).find("button[type=submit]"),
                    $(form).find("#btn-loading"),
                    {
                        formCek: formCekAddUser,
                        $modalDiv: $("#modalAddUser"),
                        $tabelData: $("#tableUserList"),
                        $btnClose: $("#modalAddUser").find(
                            '[data-dismiss="modal"]'
                        ),
                        $options: {
                            loadStats: loadStats,
                        },
                    }
                );
            },
        })
    );

    // Edit user
    $formEditUser.validate(
        $.extend(FormHelper.validasiForm(), {
            rules: {
                name: { required: true, minlength: 3, maxlength: 30 },
                email: { required: true, email: true, customEmail: true },
                password: { minlength: 8 },
                password_confirmation: {
                    equalTo: "#formEditUser input[name='password']",
                },
                perusahaan: { minlength: 3, maxlength: 30 },
                whatsapp: { number: true, minlength: 8, maxlength: 15 },
                telegram: { number: true, minlength: 8, maxlength: 15 },
                alamat: { minlength: 5, maxlength: 100 },
            },

            submitHandler: (form) => {
                if (!formCekEditUser.isChanged()) return false;
                return FormHelper.submitForm(
                    form,
                    $(form).attr("action"),
                    $(form).find("button[type=submit]"),
                    $(form).find("#btn-loading"),
                    {
                        formCek: formCekEditUser,
                        $tabelData: $("#tableUserList"),
                        $modalDiv: $("#modalEditUser"),
                        $btnClose: $("#modalEditUser").find(
                            '[data-dismiss="modal"]'
                        ),
                        $options: {
                            loadStats: loadStats,
                        },
                    }
                );
            },
        })
    );

    // reload statistik saat halaman dimuat
    loadStats();
});



